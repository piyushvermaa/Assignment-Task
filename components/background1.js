import { memo } from "react";
import PropTypes from "prop-types";

const Background1 = memo(({ className = "" }) => {
  return (
    <div>
      
    </div>
  );
});

Background1.propTypes = {
  className: PropTypes.string,
};

export default Background1;
